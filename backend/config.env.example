# Puerto del servidor FastAPI
PORT=8000

# Puntos de contacto de Cassandra (separados por comas si hay múltiples nodos)
# Formato: host:puerto,host:puerto
# Ejemplo para un solo nodo: 127.0.0.1:9042
# Ejemplo para múltiples nodos: 127.0.0.1:9042,127.0.0.1:9043,127.0.0.1:9044
CASSANDRA_CONTACT_POINTS=127.0.0.1:9042

# Datacenter de Cassandra (por defecto es "datacenter1")
CASSANDRA_DATACENTER=datacenter1

# Keyspace a usar
CASSANDRA_KEYSPACE=iot

